package entities;

public class RangedWeapon {
    protected double x, y;
    protected double vectorX, vectorY;

    public RangedWeapon(double x, double y, double vectorX, double vectorY) {
        this.x = x;
        this.y = y;
        this.vectorX = vectorX;
        this.vectorY = vectorY;
    }

    public RangedWeapon() {
    }
    
    
}
